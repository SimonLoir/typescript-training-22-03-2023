// Déclaration de module pour le module is-even
declare module 'is-even' {
    // On doit déclarer la fonction isEven comme étant exportée par défaut.
    export default function isEven(n: number): boolean;
}
